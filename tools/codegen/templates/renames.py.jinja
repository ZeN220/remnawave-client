# @generated by tools/codegen -- do not edit

from adaptix import Provider, name_mapping

{% if renames %}
from remnawave._generated.models import (
{% for model in renames %}
    {{ model }},
{% endfor %}
)
{% endif %}

NAME_MAPPING: tuple[Provider, ...] = (
{% for model, mapping in renames.items() %}
    name_mapping(
        {{ model }},
        map={
{% for python, wire in mapping.items() %}
            "{{ python }}": "{{ wire }}",
{% endfor %}
        },
    ),
{% endfor %}
)
